

const initialState={
    username:'',
    id: 0,
    profilePicture:''
}

const reducer = (state = initialState, actions) => {
    switch (actions.type) {
        default: return state
    }
}

export default reducer;